function StartScreen({children}) {
    return (
        <div className="start">
            <h2>Welcome To The React Quiz!</h2>
            {children}
        </div>
    )
}

export default StartScreen
